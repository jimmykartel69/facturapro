# Database
DATABASE_URL="postgresql://user:password@localhost:5432/facturapro"

# Session
# Session is managed via cookies, no additional env vars needed

# Optional: for email-based password reset (e.g., Resend, SendGrid)
# SMTP_HOST="smtp.resend.com"
# SMTP_PORT="465"
# SMTP_USER="resend"
# SMTP_PASSWORD="your-api-key"
# EMAIL_FROM="noreply@facturapro.app"
